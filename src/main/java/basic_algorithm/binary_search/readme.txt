二分搜索核心四点要素（必背&理解）
    1、初始化：start=0、end=len-1
    2、循环退出条件：start + 1 < end
    3、比较中点和目标值：A[mid] ==、 <、> target
    4、判断最后两个元素是否符合：A[start]、A[end] ? target

模板1：Template1 二分查找的最基础和最基本的形式 ——最简单
模板2：Template2 二分查找的高级模板。它用于查找需要访问数组中当前索引及其直接右邻居索引的元素或条件。
模板3：Template3 访问当前索引及其在数组中的直接左右邻居索引的元素或条件 ——最常用，模板2可以当做模板3的特殊场景


其实有序搜索（局部有序，全局有序）

Solution704：二分查找的demo，用模板一即可
Solution34：使用二分查找找到对应的相应位置，再往前往后找到对应的相对位置
Solution35: 注意先转化为数学的num[pos-1]< target <= num[pos]
Solution74: 先确定rol,再确定col，确定rol用模板3，确定col用模板1
Solution278:方法一：左侧是正确，右侧是错误是第一个版本的条件。直接使用二分查找。方法二：找到最左边界，对二分查找进行少量修改，不会返回mid，而是返回左侧
Solution153:用二分查找一直找非递增数列，但要考虑一种情况，就是整个数组没有进行旋转.(全局有序)
Solution154: 用二分查找一直找非递增数列，但需要注意重复值，并打破重复值时候的。同时也要考虑全局有序
Solution33: 用二分查找搜索局部有序的序列，注意的是，此时除了mid外，也要探索边界值。
Solution81: 同154，注意重复值
